# EXAMPLE — fill in values and rename to secret.yaml before applying.
# DO NOT commit secret.yaml to source control.
#
# Encode values with: echo -n "your-key" | base64
apiVersion: v1
kind: Secret
metadata:
  name: llm-api-keys
  namespace: llm-comparison
type: Opaque
data:
  # echo -n "sk-..." | base64
  OPENAI_API_KEY: ""
  # echo -n "sk-ant-..." | base64
  ANTHROPIC_API_KEY: ""
  # echo -n "..." | base64
  TOGETHER_API_KEY: ""
