apiVersion: v1
kind: Service
metadata:
  name: collector
  namespace: llm-comparison
  labels:
    app: collector
spec:
  selector:
    app: collector
  ports:
  - name: http
    port: 8001
    targetPort: 8001
    protocol: TCP
  type: ClusterIP
